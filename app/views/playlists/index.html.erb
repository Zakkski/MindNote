<%= render 'shared/navbar' %>
<% @playlists.each do |playlist| %>
  <%= link_to playlist.name, select_playlist_path(playlist), method: :patch %>
<% end %>
<%= form_for(@new_playlist) do |f| %>
  <%= f.text_field :name, placeholder: "Enter a playlist name" %>
  <%= f.submit %>
<% end %>
